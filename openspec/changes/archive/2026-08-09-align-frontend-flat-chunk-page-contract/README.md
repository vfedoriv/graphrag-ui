# align-frontend-flat-chunk-page-contract

Tighten the frontend bounded chunk-page filter contract around the backend virtual kind=FLAT selector while preserving the existing Chunk Explorer behavior.
